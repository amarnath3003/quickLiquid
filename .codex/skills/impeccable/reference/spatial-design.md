# Spatial Design

- Use a 4pt spacing scale.
- Name spacing tokens semantically.
- Use `gap` for sibling spacing.
- Prefer asymmetry and hierarchy over uniform padding.
- Avoid nested cards and repetitive card grids.
- Use container queries and optical adjustments where needed.
- Build a sensible z-index scale instead of arbitrary values.
