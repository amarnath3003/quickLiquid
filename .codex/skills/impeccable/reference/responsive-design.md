# Responsive Design

- Start mobile-first and layer up with min-width queries.
- Break on content, not device names.
- Use container queries for components where they help.
- Adapt input affordances for fine and coarse pointers.
- Handle safe areas and notches with `env()`.
- Test on real devices when possible, not only in emulation.
